import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#427EED',
        },
        secondary: {
            main: '#dc004e',
        },
        background: {
            default: '#4CC9F0',
            // paper: '#2d2d2a'
        },
    },
});

export default theme;
